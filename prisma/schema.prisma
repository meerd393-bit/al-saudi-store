generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Order {
  id           String @id
  customerName String
  phone        String
  address      String
  product      String
  color        String
  amount       String
  status       String
  date         String
}
